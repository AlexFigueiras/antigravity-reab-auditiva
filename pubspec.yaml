name: ear_training
description: "Neural Plasticity-Based Auditory Rehabilitation App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  cupertino_icons: ^1.0.6
  audio_session: ^0.1.18
  fl_chart: ^0.68.0
  get_it: ^7.7.0
  provider: ^6.1.2
  supabase_flutter: ^2.8.0
  flutter_dotenv: ^5.1.0
  http: ^1.6.0
  path_provider: ^2.1.5
  crypto: ^3.0.7
  pdf: ^3.12.0
  printing: ^5.14.3
  introduction_screen: ^4.0.0
  flutter_stripe: ^12.4.0
  permission_handler: ^12.0.1
  device_info_plus: ^13.0.0
  shared_preferences: ^2.2.0
  ffi: ^2.1.0
  flutter_tts: ^4.2.0
  google_mobile_ads: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env
    - assets/audio/
    - assets/speech/
